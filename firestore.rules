rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Temporary open rules for local testing — lock down before production
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
