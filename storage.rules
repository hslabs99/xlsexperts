rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Public read for site images. All writes go through Admin SDK (bypasses rules).
    match /{allPaths=**} {
      allow read: if true;
      allow write: if false;
    }
  }
}
